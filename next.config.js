/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "res.cloudinary.com",
      "cdn.simpleicons.org",
      "upload.wikimedia.org",
    ],
  },
};

module.exports = nextConfig;
